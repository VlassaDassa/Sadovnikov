import React from "react";


const AdminLayout: React.FC<React.ReactNode> = (children) => {
    return (
        <main>
            layout
            { children }
        </main>
    )
}

export default AdminLayout;
